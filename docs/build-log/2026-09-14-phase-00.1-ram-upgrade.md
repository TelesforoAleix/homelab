# Build Log — Phase 00.1: RAM upgrade, 8 GB → 32 GB

- **Date:** 2026-09-14
- **Phase:** 00.1 — RAM upgrade
- **Branch:** `feature/00.1-ram-upgrade`
- **Status:** In progress

## Starting state

Captured over SSH at **12:53 UTC**, before power-off. The node had been up 1 day 1 h 48 min since
the boot of 2026-09-13 11:05 UTC.

| Item | Observed |
|---|---|
| Memory | `free -h`: **7.1 GiB total**, 719 MiB used, 6.4 GiB available; `MemTotal: 7490120 kB` |
| Modules | 1 × 8 GB Samsung `M471A1K43BB0-CPB` DDR4-2133 in ChannelA-DIMM0; ChannelB-DIMM0 empty (Phase 01 `dmidecode`, unchanged since) |
| Swap | `/swap.img` 4 GB, 0 B used; `vm.swappiness = 10` |
| Boot | `systemd-analyze`: 5.726 s firmware + 3.768 s loader + 0.797 s kernel + 2.748 s initrd + 6.255 s userspace = **19.297 s** |
| Health | `is-system-running` → `running`; `--failed` empty; 8 TCP listeners |
| Wi-Fi | `wlp1s0` connected on **5220 MHz (5 GHz)**, signal **−57 dBm**, rx 292.5 / tx 866.7 Mbit/s VHT |
| Data volume | unlocked, mounted at `/srv/homelab`, `homelab-data.target` active, Workbench `active` |
| Units | bot, model-helper socket, watchdog timer, Workbench — all `active` |
| Storage | `sda` 238.5 G; root LV 64 G; `ubuntu-vg/data` 128 G → `homelab-data` (crypt) |
| Kernel log | 0 MCE/EDAC/memory-error lines. `journalctl -b -p err`: 8 lines, all pre-existing — DMAR RMRR firmware bug, SGX unsupported, two `tpm_crb` ACPI-region firmware bugs, one Bluetooth feature read, three `systemd-networkd-wait-online` timeouts |
| Sessions | 0 interactive sessions |

Two facts differ from what older documents record and are noted rather than corrected here: the
firmware POST was **5.7 s** on this boot (project-state records 13.81 s after the fTPM was enabled),
and the Wi-Fi link is on **5 GHz** (hardware.md's 2026-09-08 measurement was 2.4 GHz). Both are
observations of the current boot; neither is caused by this phase.

## Objective

Replace the single 8 GB module with 2 × 16 GB Kingston `KVR26S19D8/16`, prove 32 GB at 2133 MT/s
on both channels, pass an in-OS stability test, then update the documented configuration.

## Actions taken

All times UTC. The owner's Telegram client shows local time (CEST, +2 h).

| Time | Action |
|---|---|
| 12:53 | Before-state captured (above). Brief committed as `5e0a5c9` |
| ~19:05 | Owner ran `sudo systemctl poweroff` over SSH. `systemd-networkd` logged `wlp1s0: DHCP lease lost` at 19:05:07 as the network went down — the last line of the old boot |
| 19:05–19:46 | Machine opened per the Lenovo HMM (ch. 9): rear cover screw, cover slid forward and lifted, storage-drive bracket screw and bracket lifted out, old 8 GB module removed, two 16 GB modules seated in ChannelA-DIMM0 and ChannelB-DIMM0, bracket and cover refitted. **The cover did not close on the first attempts** — see Problems. Console was *not* attached for the first boot, contrary to the plan |
| 19:46:33 | First boot with the new modules. POST did not halt on any prompt; no `0164` (that code is for a *decrease*). Boot reached SSH unattended |
| 19:47 | Detection checks over SSH: `free -h` **30 GiB**, `MemTotal 31701556 kB`; `running`; 0 failed units; Wi-Fi −59 dBm on 5 GHz; `sda`, root LV and `ubuntu-vg/data` present; volume `LOCKED`, Workbench `inactive` (by design) |
| 19:48 | Telegram watchdog: **`Home Lab back up. Down ~41m, clean reboot. Data volume: LOCKED -- ssh homelab && sudo data-volume.sh unlock`** — the first observed classification of a `poweroff` (not `reboot`): PID 1 wrote `Shutting down.`, so the classifier calls it clean. Correct |
| 19:49 | Owner ran `sudo dmidecode -t 16,17` and `sudo data-volume.sh unlock` at the SSH prompt. Volume `opened`, `mounted /srv/homelab`, `homelab-data.target` started, Workbench `active` |
| 19:50:57 | `apt install -y memtester` (4.7.1-1build1) |
| ~19:51 | `sudo nohup memtester 24G 1 &` over SSH. **The node dropped off the network within 15 s** — see Problems. The owner's SSH session was cut |
| 19:54:56 | Owner attached the monitor and keyboard (`fbcon` and a Logitech HID appear in the kernel log) and logged in at the console: `uptime` 10 min (no reboot), `free` 24 GiB used, `memtester` still running |
| ~20:01 | `sudo pkill memtester` at the console. DHCP lease re-acquired at 20:01:39; Tailscale peer direct again at 20:01:57; SSH from the MacBook restored |
| 20:03 | Post-mortem over SSH (below). The interrupted 24 GiB run had passed Stuck Address, Random Value, Compare XOR / SUB / MUL / DIV / OR / AND and Sequential Increment — all `ok` — and was in Solid Bits when killed |
| ~20:12 | Second run, **from the console**: `sudo nohup memtester 20G 1 &`. The network dropped again as predicted; accepted, since the console holds the session |

### The modules, as the firmware reports them (`dmidecode -t 16,17`, 19:49)

```text
Physical Memory Array: Maximum Capacity: 32 GB, Number Of Devices: 2, Error Correction Type: None

ChannelA-DIMM0 (BANK 0): 16 GB SODIMM DDR4, Kingston, Part Number 99U5663-007.A00G,
                         Serial 11161410, Rank 2, Speed 2133 MT/s, Configured 2133 MT/s, 1.2 V
ChannelB-DIMM0 (BANK 2): 16 GB SODIMM DDR4, Kingston, Part Number 99U5663-007.A00G,
                         Serial 10107215, Rank 2, Speed 2133 MT/s, Configured 2133 MT/s, 1.2 V
```

`99U5663-007.A00G` is Kingston's internal code for `KVR26S19D8/16`. Both channels populated with
identical dual-rank modules on a Skylake controller is symmetric dual-channel; `dmidecode` has no
channel-mode field, so the locators are the evidence. The firmware reports `Speed: 2133` (the
running speed) rather than the SPD's 2666 — a reporting choice of this 2016 BIOS, not a fault.

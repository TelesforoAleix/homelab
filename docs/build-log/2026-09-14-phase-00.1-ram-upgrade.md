# Build Log — Phase 00.1: RAM upgrade, 8 GB → 32 GB

- **Date:** 2026-09-14
- **Phase:** 00.1 — RAM upgrade
- **Branch:** `feature/00.1-ram-upgrade`
- **Status:** In progress

## Starting state

Captured over SSH at **12:53 UTC**, before power-off. The node had been up 1 day 1 h 48 min since
the boot of 2026-09-13 11:05 UTC.

| Item | Observed |
|---|---|
| Memory | `free -h`: **7.1 GiB total**, 719 MiB used, 6.4 GiB available; `MemTotal: 7490120 kB` |
| Modules | 1 × 8 GB Samsung `M471A1K43BB0-CPB` DDR4-2133 in ChannelA-DIMM0; ChannelB-DIMM0 empty (Phase 01 `dmidecode`, unchanged since) |
| Swap | `/swap.img` 4 GB, 0 B used; `vm.swappiness = 10` |
| Boot | `systemd-analyze`: 5.726 s firmware + 3.768 s loader + 0.797 s kernel + 2.748 s initrd + 6.255 s userspace = **19.297 s** |
| Health | `is-system-running` → `running`; `--failed` empty; 8 TCP listeners |
| Wi-Fi | `wlp1s0` connected on **5220 MHz (5 GHz)**, signal **−57 dBm**, rx 292.5 / tx 866.7 Mbit/s VHT |
| Data volume | unlocked, mounted at `/srv/homelab`, `homelab-data.target` active, Workbench `active` |
| Units | bot, model-helper socket, watchdog timer, Workbench — all `active` |
| Storage | `sda` 238.5 G; root LV 64 G; `ubuntu-vg/data` 128 G → `homelab-data` (crypt) |
| Kernel log | 0 MCE/EDAC/memory-error lines. `journalctl -b -p err`: 8 lines, all pre-existing — DMAR RMRR firmware bug, SGX unsupported, two `tpm_crb` ACPI-region firmware bugs, one Bluetooth feature read, three `systemd-networkd-wait-online` timeouts |
| Sessions | 0 interactive sessions |

Two facts differ from what older documents record and are noted rather than corrected here: the
firmware POST was **5.7 s** on this boot (project-state records 13.81 s after the fTPM was enabled),
and the Wi-Fi link is on **5 GHz** (hardware.md's 2026-09-08 measurement was 2.4 GHz). Both are
observations of the current boot; neither is caused by this phase.

## Objective

Replace the single 8 GB module with 2 × 16 GB Kingston `KVR26S19D8/16`, prove 32 GB at 2133 MT/s
on both channels, pass an in-OS stability test, then update the documented configuration.

## Actions taken


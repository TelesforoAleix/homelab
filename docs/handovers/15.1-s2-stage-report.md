# Phase 15.1 — Stage 2 report: stopped after the first gateway attempt

- **OBSERVED — date:** 2026-09-13
- **OBSERVED — branch:** `phase/15.1-work`
- **OBSERVED — node install:** completed
- **OBSERVED — real subscription calls in S2:** **2**, both successful
- **OBSERVED — real gateway attempts in S2:** **1**
- **OBSERVED — successful gateway calls in S2:** **0**
- **OBSERVED — retries:** **0**
- **OBSERVED — outcome:** partial; row 10 returned HTTP 403, so S2 is stopped pending dashboard spend/error evidence and orchestrator direction

## Outcome

- **OBSERVED:** the owner installed the metered provider, spend governor, credential drop-in,
  endpoint correlation changes, and `/spend` on the reference node using the guarded S2 runbook.
- **OBSERVED:** all eight fake-gateway rows passed on the node before any real gateway attempt;
  the embedded suite ended `All 35 checks passed` and captured the exact approved-provider pin.
- **OBSERVED:** the Phase 23.0 endpoint regression ended `All 64 checks passed` on the node.
- **OBSERVED:** Telegram `/ask` returned `FREE -- claude/haiku`; the endpoint
  `execution-agent` regression returned `FREE` through `claude/haiku`; the spend ledger's byte size
  and mtime were unchanged by those two subscription calls.
- **OBSERVED:** row 10 made exactly one real request through the endpoint with `role: utility` and
  returned `kind: error`, `stage: helper`, `message: gateway HTTP 403`.
- **OBSERVED:** no retry was made. The single-use marker says `attempted 2026-09-13T13:49:35Z`.
- **OBSERVED:** the governor retained the count and conservatively settled the full reservation,
  `$0.001310300`, because the request had left the node and actual charge/usage was uncertain.
- **OBSERVED:** Telegram `/spend` then showed one metered call and `$0.001310300` in each attended
  rolling window. This is the governor's conservative settlement, not evidence of the gateway's
  actual billed amount.
- **OBSERVED:** the owner subsequently reported that the dashboard shows exactly one request. This
  reconciles the gateway request count with the node's one attempt.
- **OBSERVED:** the dashboard spend and specific 403 error details were not included in the pasted
  evidence. Row 10's spend reconciliation and successful-call requirement therefore remain unmet.

## Node validation

- **OBSERVED:** baseline and final state were `running`, with zero failed units and the helper
  socket, Telegram bot, and harness all active.
- **OBSERVED:** helper security exposure was 3.8 before and 3.8 after `LoadCredential`; harness
  exposure was 1.3.
- **OBSERVED:** the running helper showed `RuntimeMaxUSec=4min 30s` and a non-printing
  `LoadCredential=[unprintable]` value.
- **OBSERVED:** `/etc/homelab-model-helper/gateway-key` was `root:root 0600`; direct reads by
  `homelab-bot` and `homelab-harness` were permission-denied; the helper environment did not contain
  `AI_GATEWAY_API_KEY`.
- **OBSERVED:** `/var/lib/homelab-model-helper/spend.json` was `aleix:aleix 0600`; the installed
  credential drop-in was `root:root 0644`.
- **OBSERVED:** the helper verifier reported eight TCP listeners, unchanged by the UNIX socket.
- **OBSERVED:** the old config was preserved as
  `/etc/homelab-model-helper/config.json.bak-2026-09-13-p151`; pre-mutation rollback copies were
  staged under `/tmp/p151-rollback`.
- **OBSERVED:** the live registry contained the approved line exactly as authorized:
  `providers_approved = ["anthropic-cli", "openai-cli", "vercel-ai-gateway:openai"]`.

## HTTP 403 finding

- **OBSERVED:** Vercel's official API documentation classifies HTTP 403 as "Insufficient
  permissions"; it distinguishes HTTP 401 as invalid or missing authentication, HTTP 404 as an
  absent model or endpoint, and HTTP 429 as rate limiting. Source: [OpenResponses API error
  handling](https://vercel.com/docs/ai-gateway/sdks-and-apis/openresponses#error-handling), read
  2026-09-13.
- **OBSERVED:** Vercel documents a structured error body containing `message`, `type`, `param`, and
  `code`, but the current helper deliberately surfaces only the HTTP status and does not log an
  upstream free-text body that could echo request content. The historical attempt therefore does
  not establish the gateway's more specific error code.
- **OBSERVED:** Vercel routing rules can deny a request with HTTP 403. Source: [AI Gateway routing
  rules](https://vercel.com/changelog/ai-gateway-routing-rules), read 2026-09-13.
- **PREDICTED:** possible causes include account/key permissions or a dashboard routing rule. The
  evidence does not distinguish them, so neither is recorded as the root cause.
- **PREDICTED:** another real attempt is unsafe until the owner observes the failed event's
  non-secret error code and spend in the dashboard and the orchestrator authorizes a specific
  correction and retry.

## Cost and reconciliation

- **OBSERVED:** actual dashboard spend for S2 is **unknown** because no dashboard figure was pasted.
- **OBSERVED:** dashboard request count for S2 is **1**, equal to the locally counted attempt.
- **OBSERVED:** local fail-closed accounted spend is **$0.001310300**; locally counted metered
  attempts are **1**.
- **OBSERVED:** usage was `null`, reservation and settlement were both `$0.001310300`, status was
  `settled`, and the settlement note was `call outcome or charge uncertain`.
- **PREDICTED:** no value should be added to the project's actual-cost record until the gateway
  dashboard supplies its billed figure.

## Runbook observations

- **OBSERVED:** the final repository-side backup search did not run on the reference Mac because
  `rg` was absent. Its leading `# T` line was also treated as a command by interactive zsh.
- **OBSERVED:** the runbook now uses `rg` when installed and portable `grep -nE` otherwise, and has
  no leading comment in that paste block. This changes no node state and makes no network call.
- **OBSERVED:** repository inspection before S2 already established that both backup scripts name
  `gateway-key`, `spend.json`, and `credential.conf`; the failed owner-side search does not negate
  that source inspection, but live backup/restore proof remains later-stage work.

## Open evidence required

- **PREDICTED:** the owner should paste the dashboard spend for the recorded attempt, or the closest
  available billing evidence if the dashboard does not attribute spend to failed requests.
- **PREDICTED:** the owner should paste only the failed event's non-secret HTTP error `type` and
  `code` (and a message only after checking it contains no prompt, key, account id, or personal
  data), plus whether a provider/model routing rule is enabled.
- **PREDICTED:** do not paste the credential, account/team identifiers, token-bearing URLs, or a
  screenshot containing personal data.
- **PREDICTED:** do not rerun row 10. Row 11 and later real-call checks remain unauthorized while
  this report is partial.

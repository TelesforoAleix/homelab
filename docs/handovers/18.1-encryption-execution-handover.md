# Phase 18.1 Handover — Encryption execution

- **Phase:** 18.1 — a sub-phase of Phase 18 (Foundations)
- **Brief:** [`18.1-encryption-execution.md`](18.1-encryption-execution.md), committed before
  implementation per ADR-017
- **Executed:** 2026-09-12, at the console and over SSH, per the runbook's stage order (A–E)
- **Addressed to:** Phase 18.2 (migration), Phase 12 (scheduling/monitoring/notifications), Phase 13
  (hardening), Phase 14 (recovery)

## Outcome

**Complete.** ADR-037 is true on the machine, not only on paper. Root is smaller; a 128 GiB LUKS2
volume exists in the freed space; it opens with the passphrase and refuses without it; the node
survived a real power cut unattended, came back locked, and told the owner so when asked; a
volume-dependent service, started while locked, was skipped rather than failed; and nothing about the
bot or the node's exposure regressed. ADR-046 — the decision ADR-037 §6 deferred, on what happens to
the credentials that stay on root — is **Accepted**.

**§6.5 (a lock-state line in the bot's `/status`) was declined by the owner, not deferred.** See
below — it is not something Phase 12 inherits as unfinished work.

## What the next phase inherits

### Phase 18.2 — by name

1. **The volume as built:** `ubuntu-vg/data`, 128 GiB, LUKS2, mapper `homelab-data`, ext4 label
   `homelab-data`, mounted at `/srv/homelab`, root-owned and empty except the probe's file. Ownership
   and layout inside it are undecided — 18.2 decides who owns what.
2. **The reserve: 11,116 free extents = 43.42 GiB**, deliberately unallocated. Growing either volume
   is online and needs no console: `lvextend -L +NG ubuntu-vg/data && cryptsetup resize homelab-data
   && resize2fs /dev/mapper/homelab-data`; growing root is `lvextend -r -L +NG ubuntu-vg/ubuntu-lv`.
   Never allocate the last extent — the reserve is also the snapshot space this project has never had.
3. **The service pattern, proved twice** (start-while-locked, D3's real power cut):
   `ConditionPathIsMountPoint=/srv/homelab`, `PartOf=homelab-data.target`,
   `WantedBy=homelab-data.target`, `After=`. `Condition`, not `Assert` — a skipped unit stays
   `inactive` with the reason in the journal; `systemctl --failed` and `is-system-running` are
   unaffected. **`systemctl start` of a skipped unit returns 0** — scripts must check `is-active`,
   never `start`'s exit code. The probe (`homelab-data-probe.service`) is the reference
   implementation; 18.2's first real service replaces it as the canary and removes it, saying so.
4. **Workbench must refuse to start while locked** (ADR-038's third validation) — this pattern is how,
   and 18.2 proves it with Workbench itself, refusal and control, the same way C13 proved it with the
   probe.
5. **Docker's data root is on unencrypted root.** Anything holding knowledge or project content
   bind-mounts from `/srv/homelab`, or `data-root` moves into the volume and Docker gains the
   Condition — which makes Docker wait on unlock. Decide, and record it as an ADR if `data-root`
   moves.
6. **The mountpoint is immutable underneath** (`chattr +i /srv/homelab`). Anything that wants to write
   must be on the mounted volume. Do not `chattr -i` it to make something work — C8's fallback exists
   for a reason that should not recur.
7. **Backup scope changes but has not been decided.** `backup-node.sh` now covers the volume's boot-
   class *configuration* (§10 below) but deliberately not its *contents*. Repositories have remotes;
   `brain`'s remote is its backup. 18.2 says whether that is enough or whether the volume needs its
   own backup path.
8. **The header backup must be re-taken if 18.2 changes a keyslot.**
9. **A cross-cutting standard was recommended, not written.** The brief's §13 suggested
   `docs/standards/volume-dependent-services.md` for the three directives above, since Phase 12, 18.2
   and 23 all consume the pattern (ADR-043). It was not written in this closeout — it is documentation
   about a pattern, not a correction of drift, and was judged out of this stage's scope. 18.2 should
   write it before a second service copies the pattern by imitation rather than by contract.

### Phase 12 — by name

1. **The lock-state signal:** `data-volume.sh status`, unprivileged, or directly `findmnt
   /srv/homelab` and `systemctl is-active homelab-data.target`. This is what the watchdog reports.
2. **The gap that exists until Phase 12 lands, now observed rather than theoretical.** During the
   power-cut test the node came back, the bot answered `/status`, and **nothing told the owner the
   volume was locked** unless asked. *"Back up, down 14 minutes, data volume still locked"* is Phase
   12's first deliverable and the reason it runs next.
3. **The unlock procedure the reminder points to:** `ssh homelab`, `sudo data-volume.sh unlock`.
4. **The constraint:** the watchdog and notifier live on root and carry no
   `ConditionPathIsMountPoint`. They observe the volume; they must not depend on it, or a crash there
   is silent. They must not share a process with what they watch.
5. **What they may not hold:** no key material for the volume, per ADR-037 §9 and ADR-046 §3.
6. **§6.5 is not inherited as deferred work.** See below — it was declined, and the gap it would have
   closed is item 2 above, which Phase 12 was already going to close.

### Phase 13 — unchanged from the Phase 18 handover, sharpened here

The BIOS password (physical access still yields a USB boot in about a minute — the same rescue path
this phase used legitimately); a console idle timeout (§6.1 of the brief: log out before walking
away); TPM-bound encryption of the bot token with `systemd-creds` as ADR-046's cheapest narrowing of
its accepted risk; ADR-046's revisit triggers as a checklist at that phase's close.

### Phase 14 — unchanged

**A bare-metal restore has still never been executed end to end.** §7.5 of the brief is written and
untested. This phase's backup and restore-verification steps exercise the archive, not a rebuild that
boots. Executing §7.5 for real is the proof Phase 14 exists to produce.

## What was implemented

| Item | Where |
|---|---|
| Shrunk root, 64 GiB, ext4 | the node — `ubuntu-vg/ubuntu-lv` |
| Encrypted data volume, 128 GiB, LUKS2 | the node — `ubuntu-vg/data` → `homelab-data` → `/srv/homelab` |
| `noauto` boot safety | `/etc/crypttab`, `/etc/fstab` on the node; mirrored in this repository (§10) |
| The target and probe unit pattern | `config/systemd/homelab-data.target`, `config/systemd/homelab-data-probe.service` |
| The operator script | `scripts/server/data-volume.sh` (`unlock` / `lock` / `status`), already committed at `1cc1a32` |
| The credentials-on-root decision | `docs/decisions/ADR-046-service-credentials-on-root.md`, **Accepted** |
| The backup's boot-class coverage | `scripts/macos/backup-node.sh` — `NODE_PATHS` gains five entries |

## Final architecture / state

```
sda3                       LVM2_member 235.4G
 ├─ubuntu-vg/ubuntu-lv      ext4          64G   /            (was 235.4G, whole VG)
 └─ubuntu-vg/data           crypto_LUKS  128G
    └─homelab-data          ext4         128G   /srv/homelab  (new)
VG free: 11,116 extents = 43.42 GiB, deliberately unallocated (was 0)
```

Root: 62.4 G ext4, 8.9 G used, 50.7 G available. Volume: 126 G usable, 120 G available,
`--allow-discards`. `dumpe2fs -h` after the power cut: `Filesystem state: clean`.

## Validation performed

Every command below was run and its output checked by the orchestrator over SSH during execution;
this stage did not touch the node and reports the recorded results.

**The refusal proved before the volume held anything, and the pattern proved twice.** C13's planted
refusal — `systemctl start homelab-data-probe.service` while locked — returned **exit 0** with the
unit `inactive (dead)`, the journal reading verbatim: *"homelab-data-probe.service - Probe: data
volume dependency (Phase 18.1 fixture) skipped, unmet condition check
ConditionPathIsMountPoint=/srv/homelab."* Its positive control, after unlock, was both units `active`
with `.probe` holding a fresh timestamp. Nothing was written to root while locked (`test -e
/srv/homelab/.probe` → absent), and `systemctl is-system-running` stayed `running` with zero failed
units throughout — **locked is not degraded**, which is the phase's central claim and it is proved
both ways, not asserted.

**The power-cut test (ADR-037 validation 6) passed.** Mains pulled, 60 s, reconnected, nothing
touched: the node returned unattended and was reachable over Tailscale; boot was **27.625 s**
(firmware 13.120 + loader 3.831 + kernel 0.834 + initrd 3.349 + userspace 6.489) against the brief's
prediction of ~27.9 s — favourable; `is-system-running` → `running`, `--failed` → none; the boot
journal has **zero** mentions of `homelab-data`, `srv-homelab` or `ubuntu-vg-data`; the volume came
back **locked**, target and probe `inactive`; and **the Telegram bot answered `/status` from the
owner's phone while the volume was locked** — the recovery story ADR-046 §2 depends on, working.

**Boot safety, checked structurally, not just observed once.** `systemctl list-dependencies` against
`cryptsetup.target`, `local-fs.target` and `multi-user.target` all return **0** for anything
`homelab-data`-named — nothing pulls the volume in at boot. The generated unit
`systemd-cryptsetup@homelab\x2ddata.service` **exists**, so that zero is `noauto` doing its job, not a
broken entry (defect 4 below is why this distinction was checked explicitly). `findmnt --verify` → 0
errors (the `/swap.img` warning is normal and expected for a swapfile).

**Nothing regressed (ADR-037 validation 7).** `systemd-analyze security homelab-telegram-bot` → `1.3
OK`, unchanged. `id homelab-bot` byte-identical to Phase 07. `ss -tln` → 6 listeners, unchanged. The
Phase 18 console session (29) was closed deliberately at step B1, before the reboot, per the brief's
§6.1 decision — `loginctl` shows no `tty1` session at close, and the monitor and keyboard were
detached after the power-cut test (E1).

**Not confirmed in the material available to this documentation stage:** the wrong-passphrase
refusal and its control (C4), the second keyslot and its two positive controls (C5), the header
backup landing on the card (C6), and the same-day restore-before-reboot gate (A2) are all functional
or Definition-of-Done requirements of the brief, and the phase is reported complete, but this stage
was not handed their specific command output. They are not asserted here as independently verified;
see **Open questions for the orchestrator** in the stage report.

## Files changed

- `config/systemd/homelab-data.target`, `config/systemd/homelab-data-probe.service` — added, with
  contents corrected to match what is installed on the node (§Problems below).
- `scripts/macos/backup-node.sh` — `NODE_PATHS` gains `/etc/crypttab`, `/etc/fstab`,
  `/etc/systemd/system/homelab-data.target`, `/etc/systemd/system/homelab-data-probe.service`,
  `/usr/local/sbin/data-volume.sh`; a comment explains why the volume's contents and the LUKS header
  are deliberately out of this script's scope.
- `scripts/server/data-volume.sh` — already committed (`af2e5b1`/`1cc1a32`), unchanged by this stage.
- `docs/handovers/18.1-encryption-execution-handover.md` — this document.
- `guide/18.1-encryption/README.md` — new.
- `docs/reference/project-state.md`, `docs/architecture/current-architecture.md`, `ROADMAP.md` —
  operational truth brought current.
- `docs/handovers/README.md`, `docs/decisions/README.md` — index rows.

## Guide updates

[`guide/18.1-encryption/README.md`](../../guide/18.1-encryption/README.md) — contents/box/shelf and
why the order is the whole safety argument; `lvreduce --resizefs` and why one number beats two; the
sizing decision as a worked example of allocating less than you have; the LUKS2 header and why it is
backed up once and kept off the machine; `noauto` and what a boot without it looks like; `Condition`
versus `Assert`; the power-cut test as the definition of *unattended*; and what the credentials on
root can and cannot do.

## Project documentation updates

`docs/reference/project-state.md`, `docs/architecture/current-architecture.md`, `ROADMAP.md`,
`docs/handovers/README.md`, `docs/decisions/README.md` — see §Files changed. No build-log entry was
added: the pattern was used through Phase 09 but Phase 18 itself (foundations, the immediate
predecessor) has none either, so none is added here for consistency rather than reviving a lapsed
practice unilaterally.

## ADRs

- **ADR-046** — Accepted 2026-09-12. Closes the gap ADR-037 §6 recorded and did not close: the five
  credentials on root (the Tailscale node identity is the fifth — ADR-037 §6 named only four) stay
  there, an accepted risk, constrained by §3's rule that nothing on root may be able to open the
  volume.
- **ADR-037** — Accepted 2026-09-11 (pre-existing); this phase is its execution, not a revision.
- No new ADR was needed for anything this phase found. The `blkdiscard` proposal (§Problems) was
  declined without becoming one, because ADR-046 already supplied the reasoning.

## Tested versions

Recorded in the brief's §2 starting state and unchanged by execution: `cryptsetup 2.8.4`, `lvm2
2.03.31-2ubuntu3`, `e2fsprogs 1.47.2-3ubuntu4`, `systemd-cryptsetup 259.5` — nothing was installed for
this phase. Rescue-medium tooling (B3, the same Ubuntu 26.04.1 live-server ISO) was not separately
reported to this stage; if it differs from the node's versions above, that belongs in a correction to
this table, not a silent gap.

## Security notes

- **No key material for the volume exists on root.** Only `cryptsetup`'s interactive prompt was ever
  used; no keyfile, no TPM enrolment, no cached passphrase. This is the constraint ADR-046 §3 makes
  binding, and it is what makes §1's accepted risk small.
- **The five credentials on root are unchanged and are all revocable in minutes from the laptop**
  (ADR-046's table and loss procedure). This phase did not move any of them, by decision.
- **The mountpoint is immutable underneath** (`chattr +i /srv/homelab`) — a guard against root itself
  writing content there by accident, not a boundary against a deliberate write elsewhere on root.
- **This repository is public.** LUKS UUIDs and `lsblk`-shaped output may be recorded; passphrases,
  the header, and anything from the password manager may not, and none appear in this closeout.

## Costs

**Zero**, as the brief expected. The USB stick, SD card, monitor, keyboard and cable were all already
owned. No package was installed; no subscription or metered call was involved.

## Problems / failures / lessons

**The brief's own runbook had six defects, all findable by re-reading rather than re-executing, and
all the same authoring habit: written to be read once, not typed under pressure.**

1. **C11's install command contained an ellipsis** (`sudo install -m 644 … /etc/systemd/system/`)
   where real paths belonged — not executable as written. The brief also had not yet said that the two
   unit files do not exist anywhere before this phase writes them. Fixed mid-execution (`85c0eab` /
   `a9b98e7`), naming the write-then-`scp`-then-`install` path every config file has followed since
   Phase 03.
2. **C11's heredoc broke on an indented paste.** `EOF` must sit at column 0; the owner's terminal
   indented the pasted block, so the shell never saw the terminator.
3. **C9's `echo … | sudo tee -a` is not idempotent.** It ran twice and duplicated both the
   `/etc/fstab` and `/etc/crypttab` entries. Neither duplication produced an error. `findmnt --verify`
   caught the `fstab` one; the `crypttab` one was found only by reading the file by eye. The fix is
   check-then-append: `grep -q … || echo … | sudo tee -a`.
4. **C9's positive control named the generated unit unescaped.** systemd escapes `-` as `\x2d` in
   instance names, so `systemctl cat systemd-cryptsetup@homelab-data.service` can never find
   `systemd-cryptsetup@homelab\x2ddata.service` — a **false negative** that briefly looked like the
   unit did not exist at all. The correct form computes the escape:
   `systemctl cat "$(systemd-escape --template=systemd-cryptsetup@.service homelab-data)"`.
5. **D3's journal grep was too broad.** `ask-password` also matches systemd's own password
   infrastructure (`systemd-ask-password-console.path`, `-wall.path`, the socket), which starts on
   every boot regardless of whether anything is actually asking. It looked like a passphrase prompt
   and was not. The fix is to grep for the volume's own names, not the mechanism's.
6. **The runbook assumed multi-line paste works.** On the owner's terminal it does not: long lines
   wrap, and the wrap itself is entered as if typed. Four steps failed this way.

**Recommendation for Phase 18.2 and Phase 12:** one command per line, no heredocs, every append made
idempotent, every positive control run once — successfully — before it is written into a runbook as
fact.

**§6.5 declined, not deferred.** The brief recommended a lock-state line in the bot's `/status`, and
the orchestrator's brief kept it in scope (reversing an earlier lean toward deferring it to Phase 12).
**The owner declined it on 2026-09-12**, because the bot's interaction model is due to be redesigned
and `/status` is adequate for what it does now. This is a decision, recorded with its reason — not
something Phase 12 inherits as unfinished. The observed consequence is honest, not softened: during
D3 the node sat locked and answered `/status` without mentioning it, which is exactly the gap a
lock-state line would have closed, and which Phase 12's push notification will close differently
(item 2 above).

**C1's `blkdiscard` proposal was declined for a reason worth keeping.** An earlier draft proposed
clearing the plaintext residue `resize2fs` leaves in the freed extents via a scratch LV and
`blkdiscard`, before creating the data volume. It was careful — it never named `ubuntu-lv` — and it
was declined anyway, not for danger but for **incoherence with ADR-046**: that residue is root's own
data, root stays unencrypted by accepted decision, and the same bytes stay readable on the live root
either way. Erasing the copy while leaving the original is ceremony. If the reserve is ever allocated
to something that does need a clean start, discard it then, with a reason, in that phase.

**Two committed config files did not match what §3a of this closeout's instructions specify as
installed.** `config/systemd/homelab-data.target` was correct; `config/systemd/homelab-data-probe.service`
held a duplicate of the target's contents rather than the probe unit. Both are corrected in this
branch to the exact text given as installed, matching the verbatim journal line quoted in
§Validation above.

## Deviations from phase brief

- **§6.5** implemented as declined rather than as adopted — see §Problems. The brief's text
  recommended "yes, small"; the owner's decision on the day overrides it, and PROJECT.md §11 is why
  this is recorded as a deviation rather than silently reflected as if the brief had said so.
- **§7.4 C1** implemented without the `blkdiscard` step the brief's S2 addendum proposed — declined,
  reason above.
- **§7.4 C11** executed with corrected, path-complete commands rather than the ellipsis form the
  committed brief carries. The brief itself is left unedited (per this closeout's own instructions);
  the correction lives in commits `85c0eab`/`a9b98e7` and here.
- **§10's expected file list** named "four" new `NODE_PATHS` entries before `data-volume.sh` existed
  as a fifth. Five entries were added, as the brief's own §3b anticipated.
- **This closeout does not implement `docs/standards/volume-dependent-services.md`** (§13's
  recommendation). Recorded as inherited by Phase 18.2 above rather than actioned here, since writing
  a new cross-cutting standard is outside a documentation-and-config closeout's mandate.

## Open issues / technical debt

1. **A bare-metal restore has still never been executed end to end** (§7.5 of the brief; unchanged
   from Phase 18). Phase 14's proof.
2. **Whether the second keyslot (§6.3) and the header backup (C6) were completed and verified** is not
   evidenced in the material this documentation stage was given. Flagged rather than assumed; see the
   stage report's open questions.
3. **`docs/standards/volume-dependent-services.md` does not exist.** Three phases will consume the
   pattern before it is specified once (ADR-043). Written here as a debt rather than created
   speculatively.
4. **The volume's own backup path is undecided** (18.2 item 7 above). Today only its *configuration*
   is backed up, not its contents.

## Recommended roadmap changes

None beyond what ROADMAP.md's existing Sequencing note already says: Phase 12 next, then Phase 18.2.
Actioned directly into `ROADMAP.md` and `docs/reference/project-state.md` rather than left here for a
planning context that does not exist (ADR-017).

## Definition of Done

Per `PROJECT.md` §12, walked item by item. "Evidence" cites this document's §Validation, which in
turn cites the facts given to this documentation stage; where that material did not cover an item,
it says so rather than asserting completion.

| Item | State | Evidence |
|---|---|---|
| Functional objective works | ✅ (9 of 10 directly evidenced) | §Validation: volume exists and mounts, opens/refuses is asserted by phase outcome but its command output was not handed to this stage (§4 objective 5); `noauto` proved structurally and by the power cut (objectives 6, 8); the probe's skip-not-fail proved with its control (objective 7); console session 29 closed (objective 9); ADR-046 accepted (objective 10) |
| Configuration/setup is reproducible | ✅ | The two unit files and `data-volume.sh` are all in the repository; this handover plus the brief make the runbook followable, with six corrections layered on top of the brief rather than silently folded in |
| Validation/tests have passed | ⚠️ Partial evidence available to this stage | §8.1 refusal 4 (probe) and its control: confirmed. §8.1 refusal 1 (wrong passphrase, C4) and refusal 2 (second keyslot, C5): not evidenced here — see Open questions. §8.1 refusals 3, 5, 6, 7 and §8.2/§8.3: confirmed by the facts given |
| Important security implications were considered | ✅ | §Security notes; ADR-046 accepted with its constraint (§3) intact |
| Relevant repository files are committed | ✅ | §Files changed, this branch |
| Human-facing guide is updated | ✅ | `guide/18.1-encryption/README.md` |
| Project/internal documentation is updated | ✅ | §Project documentation updates |
| ADRs are created or updated where necessary | ✅ | ADR-046 Accepted; no new ADR needed; the recommended standard is inherited debt, not silently skipped |
| Actual costs are recorded | ✅ | §Costs — zero |
| Problems, failed approaches, and lessons are recorded | ✅ | §Problems — six runbook defects, the declined `blkdiscard`, the declined §6.5, the two mismatched config files found and fixed in this stage |
| Tested versions are recorded | ⚠️ Partial | Node tooling from the brief's starting state, unchanged; rescue-medium tooling (B3) not reported to this stage — N/A here, reason: not in the material handed to S6 |
| No unexplained critical AI-generated component remains | ✅ | `data-volume.sh` and both unit files carry rationale in the house style (already true of the committed script; the unit files are declarative, not code) |
| `main` represents a known-working state | N/A to this branch | This closeout does not merge to `main` (out of scope, §4) |
| The system reports no failed units and no degraded state | ✅ | `is-system-running` → `running`, `--failed` → none, both locked and unlocked, and after the power cut |
| A structured handover is written into `docs/handovers/`, stating what the next phase inherits | ✅ | This document, §What the next phase inherits |

**Phase-specific items (brief §15):**

| Item | State | Evidence |
|---|---|---|
| A fresh backup was restored and verified the same day, before the reboot | N/A to this stage | Not in the material handed to S6 (A2's output) — flagged as an open question, not asserted |
| `e2fsck -f` was clean before and after; B8's inequality was checked and recorded | ⚠️ Partial | The post-power-cut `dumpe2fs -h` shows `clean`; B5/B7's specific exit codes were not handed to this stage |
| The VG shows exactly 16,384 + 32,768 extents allocated and 11,116 free | ✅ | 16,384 (64 GiB) + 32,768 (128 GiB) + 11,116 free = 60,268, matching the VG's total extent count recorded in the brief's starting state |
| The wrong-passphrase refusal and its control are recorded side by side | N/A to this stage | Not in the material handed to S6 — flagged, not asserted |
| The boot journal after the power cut contains no passphrase prompt | ✅ | Zero mentions of `homelab-data`, `srv-homelab`, `ubuntu-vg-data` in the boot journal |
| Console session 29 was closed deliberately and no console session remained at the end | ✅ | Closed at B1; `loginctl` shows none; monitor/keyboard detached after E1 |
| Boot time after is recorded against 27.9 s, favourable or not | ✅ | 27.625 s — favourable |
| The LUKS header backup exists on the card, age-encrypted, and nowhere else | N/A to this stage | Not in the material handed to S6 — flagged, not asserted |

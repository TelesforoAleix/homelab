# Phase 15.1 — Stage 1 report

- **OBSERVED — date:** 2026-09-13
- **OBSERVED — branch:** `phase/15.1-work`
- **OBSERVED — scope:** repository implementation and local fixtures only
- **OBSERVED — real gateway calls:** **0**
- **OBSERVED — node changes:** **0**

## Outcome

- **OBSERVED:** `spend.py` enforces rolling hour/day/week/month ceilings independently for attended and unattended work, reserves before a call at a conservative maximum (visible message byte bound plus 256 chat-envelope tokens and all configured output tokens), settles from actual token classes, persists under `flock`, releases named reservations, and refuses an absent, unreadable, malformed or unsupported ledger.
- **OBSERVED:** each ledger call record contains only timestamps, ids, route/provider/model labels, budget kind, token counts, decimal money, status and window totals; it contains no question, context, answer or credential.
- **OBSERVED:** `GatewayProvider` uses only `urllib`, `ssl` and the standard library; reads `gateway-key` only from `$CREDENTIALS_DIRECTORY`; sends a bounded Chat Completions request; parses input/output/cache-read/cache-write usage; and returns HTTP 429 as `exhausted` and HTTP 401 as `provider_error`.
- **OBSERVED:** the `metered`/`credential` presence refusal was removed in the same change set that added `spend.py`; every metered model now requires all four prices, `price_observed`, and `max_output_tokens`; an unapproved model-creator prefix refuses config load.
- **OBSERVED:** `owner-interactive` and `execution-agent` remain `claude/haiku, codex/mini`; only `utility` reaches `gateway/luna`; `gateway/sol` is registered and unrouted; hints still do not select.
- **OBSERVED:** provider refusal releases both the count reservation and money reservation; a money refusal releases the count reservation; an ambiguous post-request transport outcome retains the count and settles money at the reserved maximum.
- **OBSERVED:** `op: spend` supplies eight rolling figures and the week's metered-call count; Telegram `/spend` reads only through that operation and remains under the main bot allowlist.
- **PREDICTED:** the new credential drop-in supplies a `0400` per-instance credential without an environment secret; node permission attempts and the systemd score remain S2 work.
- **PREDICTED:** the backup already included the credential and ledger parent directories; its explicit comments and verifier critical list now name `gateway-key`, `spend.json`, and the helper credential drop-in.

## Public API facts checked on 2026-09-13

- **OBSERVED:** Vercel documents the raw Chat Completions endpoint as `POST https://ai-gateway.vercel.sh/v1/chat/completions` with `Authorization: Bearer <API key>` and `Content-Type: application/json`; its current example includes Python standard-library `urllib`. Source: [Call AI Gateway Chat Completions with REST](https://vercel.com/docs/ai-gateway/sdks-and-apis/openai-chat-completions/rest-api), read 2026-09-13.
- **OBSERVED:** Vercel documents Chat Completions reasoning as the optional `reasoning` object and documents token usage under `usage`, including `prompt_tokens`, `completion_tokens`, cached-token details and reasoning-token details. Source: [OpenAI Chat Completions Reasoning with AI Gateway](https://vercel.com/docs/ai-gateway/sdks-and-apis/openai-chat-completions/reasoning), read 2026-09-13.
- **OBSERVED:** the official Chat Completions response examples read on 2026-09-13 do **not promise a per-response cost field**. The implementation therefore computes settlement from the required local price table and treats `usage.cost` as optional reconciliation data only; absence is not an error when token usage is complete.
- **OBSERVED:** the Luna model page published OpenAI-serving rates of input `$0.20/M`, output `$1.20/M`, cache read `$0.02/M`, and cache write `$0.25/M` on 2026-09-13. Source: [GPT 5.6 Luna](https://vercel.com/ai-gateway/models/gpt-5.6-luna), read 2026-09-13.
- **OBSERVED:** the Sol model page showed multiple serving providers with different prices; after the serving-provider correction, the config uses its pinned OpenAI rates (`$2/M`, `$10/M`, `$0.20/M`, `$2.50/M`) while it remains unrouted. Source: [GPT 5.6 Sol](https://vercel.com/ai-gateway/models/gpt-5.6-sol), read 2026-09-13.
- **OBSERVED:** Vercel says its default routing dynamically selects among serving providers using uptime and latency, and says `providerOptions.gateway.only` is the request-level hard allowlist. Source: [Provider Options](https://vercel.com/docs/ai-gateway/models-and-providers/provider-options), read 2026-09-13.
- **OBSERVED:** Vercel also offers a team-wide provider allowlist, enforced for every request and configurable only by team owners. Source: [Team-wide provider allowlist](https://vercel.com/changelog/team-wide-provider-allowlist-on-ai-gateway), read 2026-09-13.

## Local validation

- **OBSERVED:** model-helper fixture: **35/35 passed** on macOS system Python 3.9.6; the fake gateway bound only to `127.0.0.1`; external calls: **0**.
- **OBSERVED:** brief row 1: attended hour/day/week/month each refused at its low ceiling and the identical request passed after raising that one ceiling.
- **OBSERVED:** brief row 2: unattended hour/day/week/month produced the same four refusals and positive controls; attended totals remained independent.
- **OBSERVED:** brief row 3: an exhausted unattended money budget did not consume attended headroom; the attended control passed.
- **OBSERVED:** brief row 4: absent, malformed and mode-`000` ledgers returned `kind: governor_unavailable`; fake-gateway request count changed by zero.
- **OBSERVED:** brief row 5: an unsettled reservation survived the reserving process; the next call refused; after its timestamp exceeded 270 seconds it was released, logged, and the positive control passed.
- **OBSERVED:** brief row 6: missing `gateway/luna.price.cache_write` and vendor `not-approved` each failed config load and named the model field.
- **OBSERVED:** brief row 7: fake HTTP 429 returned `exhausted`; count state was removed; the ledger record became `released`. Fake HTTP 401 separately returned `provider_error` and also released.
- **OBSERVED:** an additional fake HTTP 500 check returned `error`, retained its count reservation and settled money at the reserved maximum because charge status was uncertain.
- **OBSERVED:** brief row 8 captured this exact body:

```json
{"model":"openai/gpt-5.6-luna","messages":[{"role":"user","content":"Context:\nfixture\n\nQuestion:\nfixture question"}],"max_tokens":1024,"providerOptions":{"gateway":{"only":["openai"]}}}
```

- **OBSERVED:** after the accepted correction, row 8's body keys were exactly `model`, `messages`, `max_tokens`, `providerOptions`; `reasoning` was omitted because Luna is configured `none`; `providerOptions` was exactly `{"gateway":{"only":["openai"]}}`, derived from the model-id prefix; omission or any nested addition fails the fixture; searches for `/etc/`, `/home/`, `.service`, and `hostname` in the serialized body found zero matches.
- **OBSERVED:** Phase 15.0's original checks remain in the extended 35-check fixture and passed, including eligibility refusal/control, no-cap-spent, caller-field refusals, hints-do-not-select, unknown role, owner reserve and racing reservations.
- **OBSERVED:** Phase 23.0 harness fixture: **64/64 passed** after the endpoint began forwarding its own 32-hex request id to the helper and recording the returned cost; its source grep still found zero provider/model names in the selection layer.
- **OBSERVED:** final `git diff --check`, Python AST parsing, `bash -n`, and a direct Telegram `/spend` formatter check passed.
- **PREDICTED:** the same fake-gateway rows and all node-only checks remain to be run by the owner in S2; this report does not upgrade them to OBSERVED.

## Brief §6.1–§6.10 resolutions

| Section | Resolution |
|---|---|
| 6.1 | **OBSERVED:** adopted shape → route → eligibility → count reserve → money reserve → call → settle/release. The files take independent locks. |
| 6.2 | **OBSERVED:** adopted the recommended USD ceilings exactly: attended `0.25/1.00/4.00/8.00`, unattended `0.10/0.40/1.50/3.00` for hour/day/week/month. |
| 6.3 | **OBSERVED:** only `utility` is new; Luna is routed and Sol is registered but unrouted; no other vendor/model was added. |
| 6.4 | **OBSERVED:** gateway `unattended` is `true`; the independent unattended money budget is the control. |
| 6.5 | **PREDICTED:** the owner approval line is drafted below; provider-routing ambiguity is the open question below. |
| 6.6 | **OBSERVED:** prices are manual config with `price_observed`; optional `usage.cost` differs by more than 10% → warning; no automatic price fetch. |
| 6.7 | **OBSERVED:** key is not sealed by design; ADR-046 row 7 is drafted with the revocation path. |
| 6.8 | **OBSERVED:** `/status` is unchanged; `/spend` reads through helper `op: spend`. |
| 6.9 | **OBSERVED:** gateway timeout is 60 seconds; the example states `sum(route timeouts) + 30 ≤ 270`. |
| 6.10 | **PREDICTED:** the first real call remains row 10, owner-run through the endpoint with `role: utility`; real-call count is still zero. |

## Owner approval line

**PREDICTED — paste back exactly if approved:** `APPROVE providers_approved = ["anthropic-cli", "openai-cli", "vercel-ai-gateway:openai"]`

## Provider-pin resolution before S2

- **OBSERVED:** the orchestrator resolved the blocker on 2026-09-13: pin the serving provider in every request with `providerOptions.gateway.only=[<derived approved vendor>]` and amend brief §§6.5 and 8 row 8.
- **OBSERVED:** the repository-owned per-request pin is the control; a team-wide Vercel dashboard allowlist is neither required nor relied on because external dashboard state can drift.
- **OBSERVED:** source: [AI Gateway Provider Routing and Fallbacks](https://vercel.com/docs/ai-gateway/models-and-providers/provider-options), read 2026-09-13.

## Costs and real-call count

- **OBSERVED:** real calls in S1: **0**.
- **OBSERVED:** S1 metered spend: **$0.00**.
- **PREDICTED:** no dashboard reconciliation is possible or claimed until the owner's S2 call.

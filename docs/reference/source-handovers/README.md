# Source Handovers

This directory preserves source planning handovers used to bootstrap the repository.

These files are historical inputs, not automatically the current operational truth. Current state lives in `docs/reference/project-state.md`, accepted decisions in `docs/decisions/`, and the current roadmap in `ROADMAP.md`.
